name: core
description: GreenLoop core package.
version: 1.0.0
publish_to: 'none'
resolution: workspace
environment:
  sdk: ^3.9.2
dependencies:
  network: ^1.0.0
  data_models: ^1.0.0
  flutter:
    sdk: flutter
dev_dependencies:
  test: ^1.24.0
